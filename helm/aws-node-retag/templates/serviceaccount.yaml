apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aws-node-retag.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ required "irsaRoleArn is required — see iam/ for setup instructions" .Values.irsaRoleArn | quote }}
